import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:mobi_plataforma_fatec/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SquadController squad;
  //
  setUp(() {
    //     squad = HomeModule.to.get<SquadController>();
  });

  group('SquadController Test', () {
    //   test("First Test", () {
    //     expect(squad, isInstanceOf<SquadController>());
    //   });

    //   test("Set Value", () {
    //     expect(squad.value, equals(0));
    //     squad.increment();
    //     expect(squad.value, equals(1));
    //   });
  });
}
