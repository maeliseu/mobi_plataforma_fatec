import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:mobi_plataforma_fatec/app/modules/home/subpages/squad/squad_page.dart';

void main() {
  testWidgets('SquadPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SquadPage(title: 'Squad')));
    //  final titleFinder = find.text('Squad');
    //  expect(titleFinder, findsOneWidget);
  });
}
