import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('VitrinePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VitrinePage(title: 'Vitrine')));
    //  final titleFinder = find.text('Vitrine');
    //  expect(titleFinder, findsOneWidget);
  });
}
